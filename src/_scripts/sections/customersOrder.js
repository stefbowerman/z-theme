import BaseCustomersSection from './baseCustomers';

export default class CustomersOrderSection extends BaseCustomersSection {
  constructor(container) {
    super(container, 'customersOrder');
  }
}
